import UIKit

enum CustomColors {
  static var mainColor: UIColor { return UIColor(named: "mainColor") ?? UIColor() }
  static var launchColor: UIColor { return UIColor(named: "launchColor") ?? UIColor() }
  static var border: UIColor { return UIColor.darkGray }
}
